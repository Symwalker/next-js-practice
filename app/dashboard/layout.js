export default function DashboardLayout({ children }) {
    return (
      <html lang="en">
        <body >
          <div>
            this is Dashboard Layout
          </div>
          {children}</body>
      </html>
    )
  }